Week 1:

weather data - Manasvin
tariff data - Mihir
household data - Shubhan

for each data:
    input : longitude, lattitude
    function: extract weekly (min, max,avg) aggregates of data

    for example: from daily weather data -> we extract daily max, min, avg -> aggregate it weekly (we will have weekly_max_data, weekly_min_data, weekly_max_data)


    output : returns this weekly_max_data, weekly_min_data, weekly_max_data

final output csv columns:

week number | weekly_max_T | weekly_min_T | weekly_avg_T |
